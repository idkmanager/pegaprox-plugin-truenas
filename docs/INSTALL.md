# Install

See the root `README.md` → "Deploy" section and `install.sh`/`uninstall.sh`.
This file exists to match the brief's documented layout (§7); the
authoritative instructions live in the root README so they don't drift out
of sync with the scripts.
